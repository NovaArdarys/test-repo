apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen-service
  namespace: prod
  labels:
    app: kitchen-service
spec:
  replicas: 2 # banyaknya replika
  selector:
    matchLabels:
      app: kitchen-service
  template:
    metadata:
      labels:
        app: kitchen-service
    spec:
      serviceAccountName: default-sa
      containers:
        - name: kitchen
          image: rahvanna/kitchen-service:latest
          ports:
            - containerPort: 3006
              name: http
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: common-secret
          env:
            - name: PORT
              value: '3006'
            - name: SERVICE_NAME
              value: 'kitchen-service'
          resources:
            requests: # minimum CPU (100m) dan memori (64Mi)
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 125m
              memory: 192Mi
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3006
          #   initialDelaySeconds: 5
          #   periodSeconds: 10
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3006
          #   initialDelaySeconds: 15
          #   periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  namespace: prod
  labels:
    app: kitchen-service
spec:
  selector:
    app: kitchen-service
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
      name: http
  type: ClusterIP
