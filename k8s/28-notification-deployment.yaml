apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: prod
  labels:
    app: notification-service
spec:
  replicas: 2 # banyaknya replika
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      serviceAccountName: default-sa
      containers:
        - name: notification
          image: rahvanna/notification-service:latest
          ports:
            - containerPort: 3008
              name: http
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: common-secret
          env:
            - name: PORT
              value: '3008'
            - name: SERVICE_NAME
              value: 'notification-service'
          resources:
            requests: # minimum CPU (100m) dan memori (64Mi)
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 125m
              memory: 192Mi
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3008
          #   initialDelaySeconds: 5
          #   periodSeconds: 10
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3008
          #   initialDelaySeconds: 15
          #   periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: prod
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
      name: http
  type: ClusterIP
