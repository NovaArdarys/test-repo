apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: prod
  labels:
    app: delivery-service
spec:
  replicas: 2 # banyaknya replika
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      serviceAccountName: default-sa
      containers:
        - name: delivery
          image: rahvanna/delivery-service:latest
          ports:
            - containerPort: 3005
              name: http
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: common-secret
          env:
            - name: PORT
              value: '3005'
            - name: SERVICE_NAME
              value: 'delivery-service'
          resources:
            requests: # minimum CPU (100m) dan memori (64Mi)
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 125m
              memory: 192Mi
          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3005
          #   initialDelaySeconds: 5
          #   periodSeconds: 10
          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3005
          #   initialDelaySeconds: 15
          #   periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: prod
  labels:
    app: delivery-service
spec:
  selector:
    app: delivery-service
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
      name: http
  type: ClusterIP
