services:
  school_service:
    build:
      context: ../..
      dockerfile: packages/school-service/Dockerfile
    container_name: school_service
    env_file:
      - ../../.env
      - .env
    environment:
      - NODE_ENV=development
      - SERVICE_INTERNAL_PORT=3010
      - SERVICE_HOST_PORT=3010
    ports:
      - "3010:3010"
    volumes:
      - ../..:/app:cached
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3010/api/health  || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
